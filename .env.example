# App identity / native build config
# APP_VARIANT controls derived native identifiers and assets:
# development => .dev / -dev, preview => .preview / -preview, production => no suffix
APP_VARIANT=production

# Base values. app.config.ts appends the APP_VARIANT suffix where needed.
EXPO_APP_NAME="Ultra Companion"
EXPO_APP_SLUG="ultra-companion"
EXPO_APP_SCHEME="ultra"
EXPO_IOS_BUNDLE_IDENTIFIER="com.conqeror.ultracompanion"
EXPO_ANDROID_PACKAGE="com.ultra.companion"

# Optional exact asset overrides. By default the config uses:
# icon.png, icon-dev.png, icon-preview.png
# splash-icon.png, splash-icon-dev.png, splash-icon-preview.png
# EXPO_APP_ICON="./assets/images/icon.png"
# EXPO_SPLASH_IMAGE="./assets/images/splash-icon.png"
# EXPO_SPLASH_BACKGROUND_COLOR="#0E0E0C"

# Optional: only needed for EAS cloud builds / updates. Create your own with `eas init`.
EXPO_EAS_PROJECT_ID=

# Mapbox tokens — get yours at https://account.mapbox.com/access-tokens/
# Download token: used at build time to fetch the Mapbox SDK (needs Downloads:Read scope)
RNMAPBOX_MAPS_DOWNLOAD_TOKEN=sk.your_download_token_here
# Access token: used at runtime for map rendering (needs default public scopes)
MAPBOX_ACCESS_TOKEN=pk.your_access_token_here

# Google Places API key — used at runtime for gas station & grocery POI data
# Get one at https://console.cloud.google.com/apis/credentials (enable Places API New)
# If this key is restricted by iOS bundle identifier, allow every local build variant you use:
# com.example.app, com.example.app.dev, and com.example.app.preview.
GOOGLE_PLACES_API_KEY=your_api_key_here
